<!-- the middle contents -->
<div class="contents">
	<section id="search-container">
		<div class="container">
			<div id="search-box">
				<h1>Your search resulted in <strong><%= @julie.total_count %></strong> gigs.</h1>
				<%= form_tag search_gigs_path, method: :get do |f| %>
					<%= text_field_tag :search, nil, placeholder: "Example: Software Developer", class: "col-md-9" %>
					<%= submit_tag "Search", class: "col-md-3 btn btn-primary" %>
				<% end %>

			</div>
		</div>
	</section>
	
	<%= render 'layouts/gig_listings' %>
</div><!-- /.contents -->
